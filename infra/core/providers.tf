provider "upcloud" {
  # Recommended: 
  # Authenticate via environment variables:
  #   export UPCLOUD_USERNAME="your-api-user"
  #   export UPCLOUD_PASSWORD="your-strong-password"
  #
  # If you’re using an API-only subaccount, those creds go here.
  # or even better, use an API token as an environment variable:
  #   export UPCLOUD_TOKEN="upctl-generated-token"
  #
  # You can optionally tune retries / timeouts if needed:
  # request_timeout_sec = 120
  # retry_max           = 5


}
